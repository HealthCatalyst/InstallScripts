apiVersion: v1
kind: PersistentVolume
metadata:
  name: file-volume-rabbitmq
  namespace: fabricrealtime
  labels:
    type: local  
spec:
  #persistentVolumeReclaimPolicy: Delete
  storageClassName: default
  capacity:
    storage: 8Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data/fabricrealtime"
  claimRef:
    namespace: fabricrealtime
    name: az-files-rabbitmq
