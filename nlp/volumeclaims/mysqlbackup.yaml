kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: az-files-mysql-backup
  namespace: fabricnlp  
spec:
  storageClassName: default
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 16Gi
