kind: PersistentVolume
apiVersion: v1
metadata:
  name: file-volume-mysql
  namespace: fabricnlp    
  labels:
    type: local
spec:
  storageClassName: default
  capacity:
    storage: 8Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"
  claimRef:
    namespace: fabricnlp
    name: az-files-mysql
