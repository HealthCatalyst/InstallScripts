apiVersion: v1
kind: PersistentVolume
metadata:
  name: file-volume-solr
  namespace: fabricnlp
  labels:
    type: local  
    namespace: fabricnlp
spec:
  #persistentVolumeReclaimPolicy: Delete
  storageClassName: default
  capacity:
    storage: 32Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"
  claimRef:
    namespace: fabricnlp
    name: nlp.solrserver
