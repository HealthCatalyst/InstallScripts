#!/bin/sh
set -e

swarmmanager="$1"
echo "Version 1.01"

# join the gluster
sudo gluster peer probe "$swarmmanager"

sudo gluster peer status

sudo mount -t glusterfs fabricrealtime2:/hcshared /mnt/hcshared

